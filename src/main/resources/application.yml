spring:
  main:
    allow-bean-definition-overriding: true

  data:
    web:
      pageable:
        one-indexed-parameters: true
        default-page-size: 10
        max-page-size: 1000

---
spring:
  profiles: local

  data:
    mongodb:
      host: 127.0.0.2
      port: 27017
      database: test
      username: testUser
      password: xyz123